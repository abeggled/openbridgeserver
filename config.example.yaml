server:
  host: 0.0.0.0
  port: 8080
  log_level: INFO

mqtt:
  host: localhost
  port: 1883
  username: null
  password: null

database:
  path: /data/opentws.db
  history_plugin: sqlite   # sqlite | influxdb | timescaledb | questdb

ringbuffer:
  storage: memory           # memory | disk
  max_entries: 10000

security:
  jwt_secret: changeme      # In Produktion: aus Env-Var OPENTWS_SECURITY__JWT_SECRET
  jwt_expire_minutes: 1440
